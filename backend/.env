# ============================================================
# backend/.env — LOCAL DEVELOPMENT ONLY (non-Docker)
# ============================================================
# This file is for running Django directly (without Docker).
# When running with Docker, docker-compose.yml overrides all values below.
# DO NOT add mssql here if Docker uses postgresql — Docker env vars always win.
# ============================================================

# Django Core
SECRET_KEY=django-insecure-%@=k9!@3knt3g30!_f+dbka@hv@pj51sxy$4(+6yh4u6l4)zdr
DEBUG=True
ALLOWED_HOSTS=localhost,127.0.0.1,*

# Database — local non-Docker development (mssql for Windows local dev)
DB_ENGINE=mssql
DB_NAME=JRMSUKatipunanCampusLibrary
DB_USER=
DB_PASSWORD=
DB_HOST=localhost
DB_PORT=1433
DB_MSSQL_DRIVER=ODBC Driver 17 for SQL Server
DB_WINDOWS_AUTH=True

# Disable SSL redirect for local dev
DISABLE_SSL_REDIRECT=True
